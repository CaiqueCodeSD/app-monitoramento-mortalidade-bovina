<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyFirstApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your theme here to be the same as light mode. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/backgroundWarm</item>
        <item name="colorSurface">@color/surfaceWarm</item>
    </style>
</resources>